[pytest]
# Adds the repo root to sys.path so `from ai_service...` and
# `from app...` imports resolve when running `pytest` from VERIDEX/,
# without needing to manually export PYTHONPATH.
pythonpath = .
testpaths = tests
